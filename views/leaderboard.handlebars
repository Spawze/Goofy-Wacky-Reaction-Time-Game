<section class="container">
    <ul class="collection with-header blue-grey darken-1">
        <li class="collection-header center">Leaderboard</li>
        {{#each scoreData as |score|}}
        {{> scoreboard-list}}
        {{/each}}
    </ul>
</section>
{{!-- 
 <div class="row-ta">
    <div class="col s6">
        <div class="card blue-grey darken-1">
            <div class="card-content white-text">
                <span id="leaderboard" class="card-title center">LEADERBOARD</span>
                {{#each scoreData as |score|}}
                {{> scoreboard-list}}
                {{/each}}
            </div>
        </div>
    </div>
</div>  --}}